[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "python scripts/migrate.py && uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1 --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
